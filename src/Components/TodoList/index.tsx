import { Button, Typography } from "@material-ui/core";
import React from "react";

export class TodoList extends React.PureComponent {
  render() {
    return <Typography variant="h3" >List</Typography>;
  }
}
